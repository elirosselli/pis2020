// istanbul ignore file
import {
  initialize,
  login,
  getToken,
  refreshToken,
  logout,
} from './interfaces';
import { getParameters, setParameters } from './configuration';

export {
  initialize,
  login,
  getToken,
  getParameters,
  refreshToken,
  logout,
  setParameters,
};
