// istanbul ignore file
import { initialize, login, logout, getToken } from './interfaces';
import { getParameters, setParameters } from './configuration';

export { initialize, login, getToken, getParameters, setParameters, logout };
