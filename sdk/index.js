// istanbul ignore file
import {
  initialize,
  login,
  logout,
  getToken,
  getUserInfo,
  refreshToken,
} from './interfaces';

import { getParameters } from './configuration';

export {
  initialize,
  login,
  logout,
  getParameters,
  getToken,
  getUserInfo,
  refreshToken,
};
